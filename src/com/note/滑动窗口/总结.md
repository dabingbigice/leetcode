```
for (int l = 0, r = 0; r < t.length(); r++) {
  //纳入新元素，增大右窗口
  while (l <= r && check()) {
       //无法增大右窗口，且l与r未相遇，删除老元素，收缩左窗口
      l++;
  }

check():判断是否 右窗口 是否还能增大，
如果不能增大则需要进入循环，缩短左窗口，
直到右窗口符合条件继续向前滑动
```